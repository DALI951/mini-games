// Default widget test placeholder — keeps `flutter create` in CI from
// generating its own (broken) copy when the android/ scaffold is created.
// The real coverage lives in home_test.dart / settings_test.dart /
// tic_tac_toe_test.dart / version_test.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test suite entrypoint', () {
    expect(1 + 1, 2);
  });
}
